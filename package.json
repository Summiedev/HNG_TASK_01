{
  "name": "profile-api",
  "version": "1.0.0",
  "description": "Profile aggregation API — Vercel + MongoDB",
  "dependencies": {
    "mongodb": "^6.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
